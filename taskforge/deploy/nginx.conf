# TaskForge Nginx Configuration
# Place at: /etc/nginx/sites-available/taskforge

server {
    listen 80;
    listen [::]:80;
    server_name taskforge.example.com;
    root /var/www/taskforge/public;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";

    index index.php;
    charset utf-8;

    # Redirect to HTTPS (uncomment after SSL setup)
    # return 301 https://$server_name$request_uri;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }

    error_page 404 /index.php;

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_hide_header X-Powered-By;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}

# HTTPS server block (after certbot)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name taskforge.example.com;
#     root /var/www/taskforge/public;
#
#     ssl_certificate /etc/letsencrypt/live/taskforge.example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/taskforge.example.com/privkey.pem;
#
#     # ... same location blocks as above ...
# }
